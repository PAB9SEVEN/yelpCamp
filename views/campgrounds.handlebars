{{> header }}





<div class="container">
<header class="jumbotron"><h2>Welcome to YELP camp</h2>
    <div class="container">
    <p>I hope you enjoy the ride</p>
    <p>
        <a class="btn btn-primary btn-large" href="/campgrounds/new">Add new campground</a>
</p>
        </div>
</header>

<div class="row text-center" style="display:flex;flex-wrap:wrap;">   
{{#each camps}}
<div class="col-md-3 col-sm-6">
    <div class="thumbnail">

    <img src="{{image}}">
        <div class="caption">
                <h4>{{name}}</h4>
        </div>
        <p>
        <a href="/campgrounds/{{_id}}" class="btn btn-primary">More Info</a></p>
    </div>
    
    </div> 
</div>
</div>
{{/each}}
     </div>
{{> footer }}
   
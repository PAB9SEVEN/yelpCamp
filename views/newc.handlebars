{{> header}}
<div class="container">
    <div class="row">
<h2 style="text-align:center;">Add new Comment to the {{data.name}}</h2>
        <div style="width:30%;margin:0 auto;">
<form action="/campgrounds/{{data.id}}/comments" method="POST">
    <div class="form-group">
<input class="form-control" type="text" placeholder="text" name="text">
        </div>

   
    <div class="form-group">
    <button class="btn btn-lg btn-default btn-block btn btn-primary">Submit!</button>
    </div>

</form>
            <a href="/campgrounds">Campgrounds</a>
            </div>

    </div>
    </div>
{{> footer}}
